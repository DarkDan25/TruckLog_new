ktor {
    deployment {
        port = 8080
        port = ${?PORT}
    }
    application {
        modules = [ com.zyablik.ApplicationKt.module ]
    }
}

database {
    driver = "org.postgresql.Driver"
    url = "jdbc:postgresql://localhost:5432/trucklog"
    user = "postgres"
    password = "zhukovdd"
}

jwt {
    secret = "trucklog_secret_key_2026"
    issuer = "http://0.0.0.0:8080"
    audience = "http://0.0.0.0:8080/trucklog"
    realm = "Access to TruckLog API"
}
